name: Build and Test

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build:
    uses: truc3651/github-workflows/.github/workflows/build.yml@master
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read