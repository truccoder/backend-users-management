ENVIRONMENT: localdev
server:
  port: 8090

app:
  domain: localhost:8090

AURORA_HOST: localhost
AURORA_PORT: 5433
AURORA_USERNAME: postgres
AURORA_PASSWORD: postgres
AURORA_DATABASE: users_localdev
AURORA_SCHEMA: users

NEO4J_HOST: localhost
NEO4J_PORT: 7687
NEO4J_USERNAME: neo4j
NEO4J_PASSWORD: neo4jpassword
NEO4J_DATABASE: neo4j

POD_NAME: abc

cache:
  host: localhost
  port: 6379

spring:
  kafka:
    bootstrap-servers: localhost:9093

keycloak:
  server-url: http://localhost:8180
  realm: users
  client-id: backend-users
  client-secret: backend-users-secret

minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket: users-profile-pictures

aws:
  ses:
    sender-email: no-reply@localhost
    localstack-endpoint: http://localhost:4566
